pub const ASPECT_RATIO: f64 = 4.0 / 3.0;
pub const IMAGE_WIDTH: i32 = 800;
pub const IMAGE_HEIGTH: i32 = (IMAGE_WIDTH as f64 / ASPECT_RATIO) as i32;
pub const RAYS_PER_PX: i32 = 100;
pub const MAX_DEPTH: i32 = 100;
